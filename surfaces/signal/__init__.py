"""Signal surface — thin client for a local signal-cli HTTP daemon
(W4 T2). Requires ``signal-cli daemon --http`` with a linked
account; JSON-RPC send + SSE receive. Maximally private, ops-fragile by
nature (the daemon is the operator's responsibility)."""
