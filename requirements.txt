# Server install set — the SAME pyproject extras scripts/deploy_prod.sh installs.
#
# The dependency SSOT is pyproject.toml; the exact pins are requirements.lock
# (`uv pip compile pyproject.toml --all-extras --universal -o requirements.lock`,
# every extra pinned, none installed unless named). There is nothing to
# regenerate here: this file only names the extras and points at the lock.
#
# Install from the repo root (paths below are relative to the CURRENT directory):
#     pip install -r requirements.txt
#
# A lighter install picks its own extras, e.g. `pip install -c requirements.lock ".[server]"`.
-c requirements.lock
.[server,browser,crypto,solana,telegram,twitter,voice]
