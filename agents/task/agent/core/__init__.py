"""Agent core mixins.

Pure code-motion extraction of internal method-groups from
``agents/task/agent/service.py`` (class ``Agent``). The mixins here are composed
back into ``Agent`` via inheritance; method bodies are verbatim.
"""
